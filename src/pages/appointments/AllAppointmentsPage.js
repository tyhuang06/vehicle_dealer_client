import React from 'react';
import { useNavigate } from 'react-router-dom';

const AllAppointmentsPage = () => {
	const navigate = useNavigate();
	return <div>All Appointments Page</div>;
};

export default AllAppointmentsPage;
